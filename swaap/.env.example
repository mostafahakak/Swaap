# Base URL of swaap-backend (no trailing slash). Required for profile DB check, onboarding, and event registration.
# Local dev:
NEXT_PUBLIC_API_URL=http://localhost:4000
#
# Production builds use `.env.production` (https://swaap.onrender.com). On Render, set CORS_ORIGIN to your Firebase Hosting / site URL(s).
